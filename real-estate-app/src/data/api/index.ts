// API interfaces and client implementations
// This directory will contain API client implementations when backend is ready

// For now, export placeholder interfaces
export interface ApiClient {
  // Will be implemented when backend is ready
}

// Placeholder for future API implementations
export const apiClient: ApiClient = {
  // Mock implementation
};